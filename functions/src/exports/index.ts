/**
 * Exportaciones CSV de Admira.
 *
 * Pendiente de implementación: generación de CSV por consolidación, snapshot
 * inmutable, empaquetado ZIP y persistencia en `csvExports` + Cloud Storage.
 * La regla de `RETAILERS` aún no está definida y no debe inventarse.
 */
export {};
